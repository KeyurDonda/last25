<table class="table table-striped table-hover table-condensed">
<tbody>
  <% Scan.where(url: @url).each do |scan| %>
    <tr>

      <td><a href="<%= scan_path(scan) %>"><%= scan.url %></a></td>

      <td><%= scan.created_at %></td>

    </tr>
  <% end %>
</tbody>
</table>